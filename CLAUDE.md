Use `uv` for python package management and running scripts.
Follow PEP8 for code style. Use `ruff` for linting and formatting.
Put imports in alphabetical order (standard library, third-party, local) and at the top of the file.
